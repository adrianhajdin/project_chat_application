# Open Source Contributions Shortcuts with GitHub CLI

- `gh repo fork <username/repo-name> --clone`
- `git pull && npm install`
- `git checkout -b <branch-name>`
- Contribute your changes
- `git commit -am "<commit message>" && git push`
- `gh pr create -w`
